export const adder = (a: number, b: number) => {
  return a + b;
};

export const suber = (a: number, b: number) => {
  return a - b;
};

export const muler = (a: number, b: number) => {
  return a * b;
};

export const diver = (a: number, b: number) => {
  return a / b;
};
